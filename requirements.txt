# Runtime deps for the PUBG UC / Spark plugin.
# NOTE: FunPayCardinal already ships requests, FunPayAPI and pyTelegramBotAPI,
# so under FPC nothing extra is usually needed. Listed here for standalone dev
# and CI. sqlite3 is part of the Python standard library.
requests>=2.28
# python-dotenv is OPTIONAL - the plugin has a built-in .env parser and does not
# require it. Install only if you also keep a CWD-level .env for other tools.
# python-dotenv>=1.0

# Dev / tests only:
pytest>=7.0
